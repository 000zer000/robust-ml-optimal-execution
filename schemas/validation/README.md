# Simulator-validation schemas

`simulator-validation-report-v1.schema.json` defines the machine-readable Step 10 Gate B report.
A `pass` decision certifies the tested exact synthetic mechanics and designed-process directional
responses only. It is not a claim of historical calibration, venue fidelity, or live profitability.
