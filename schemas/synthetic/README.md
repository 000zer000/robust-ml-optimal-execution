# Synthetic-market contracts

`synthetic-market-config-v1.schema.json` defines the machine-readable Step 9 scenario contract.
`synthetic-market-manifest-v1.schema.json` defines the immutable provenance and output summary.

The manifest must state `not_calibrated_step9`. Historical calibration belongs to later data/replay
steps and cannot be inferred from a designed synthetic parameter set.
